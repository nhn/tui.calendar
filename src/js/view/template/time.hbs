{{#each matrices}}
    {{#each this}}
        {{#each this}}
            {{#if this}}
            <div class="view-time-date-event-block" data-id="{{stamp model}}" style="{{time-eventBlock this}}">
                <div class="view-time-event">{{model.title}}</div>
                <div class="view-time-resize-handle">=</div>
            </div>{{/if}}
        {{/each}}
    {{/each}}
{{/each}}
