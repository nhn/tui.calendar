+prefix-classes(css-prefix)
    .list-dot
        display: inlin-block;
        position: relative;
        top: -1px;
        content: '';
        width: 7px;
        height: 7px;
        border-radius: 50%;
